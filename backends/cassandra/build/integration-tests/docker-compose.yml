version: '3'
services:
   cassandra:
     image: com.expedia.www.haystack.trace.storage.backends.cassandra:3.11.0
     environment:
       MAX_HEAP_SIZE: 256m
       HEAP_NEWSIZE: 256m
     ports:
       - "9042"
